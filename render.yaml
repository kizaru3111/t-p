services:
  - type: web
    name: pdf-telegram-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: PORT
        value: 10000
      - key: BOT_TOKEN
        sync: false
    autoDeploy: true
